// Ejercicio 11

const num = parseInt(prompt('Introduzca un número, por favor.'));
if(num % 2 === 0) {
    document.write(`El ${num} es divisible por 2`);
} else {
    document.write(`El ${num} NO es divisible por 2`);
}
